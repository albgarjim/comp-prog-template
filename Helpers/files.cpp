#include <stdio.h>
    #include <queue>
    #include <string.h>
    #include <iostream>
    #include <fstream>
    #include <string>
    #include <vector>
    #include <algorithm>
    #include <map>
    #include <set>
    #include <list>
    #include <iomanip>
    #include <math.h>
    #include <sstream>
    #include <numeric>
    #include <stdlib.h>
    #include <time.h>
    #define GET_VARIABLE_NAME(Variable) (#Variable)
    using namespace std;
    #define el "\n"

//----------------------------------------------------------------

ifstream ifs("test.in", ifstream::in);
ofstream ofs("test.out", ofstream::out);

// ifstream ifs("data/a_example.txt", ifstream::in);
// ofstream ofs("data/a_example.out", ofstream::out);

// ifstream ifs("data/b_lovely_landscapes.txt", ifstream::in);
// ofstream ofs("data/b_lovely_landscapes.out", ofstream::out);

// ifstream ifs("data/c_memorable_moments.txt", ifstream::in);
// ofstream ofs("data/c_memorable_moments.out", ofstream::out);

// ifstream ifs("data/d_pet_pictures.txt", ifstream::in);
// ofstream ofs("data/d_pet_pictures.out", ofstream::out);

// ifstream ifs("data/e_shiny_selfies.txt", ifstream::in);
// ofstream ofs("data/e_shiny_selfies.out", ofstream::out);
